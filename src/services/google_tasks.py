"""Removed in the Phase 1 refactor.

All previous functionality lives in ``adapters.gcloud_tasks`` (readers and
writers) and ``adapters.gcloud_auth`` (OAuth helpers). This file is kept as
an empty stub only because the Cowork sandbox cannot delete files; please
remove it from the working tree:

    git rm src/services/google_tasks.py
"""
