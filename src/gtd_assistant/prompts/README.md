# Prompts

English captures use a classify prompt first, then a type-specific enrich prompt.
Spanish captures use the single Spanish prompt. Change classify prompts for type
selection errors; change enrich prompts for extracted field errors.
