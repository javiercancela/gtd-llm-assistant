# Copy to ~/.config/gtd-llm-assistant/env (chmod 600) for launchd runs.
# launchd does not load your shell profile; the wrapper script sources this file.
GEMINI_API_KEY=

# Google Tasks list IDs (run: uv run python -c "from adapters.gcloud_tasks import list_tasklists; ...")
# GTD_TASKLIST_PERSONAL=
# GTD_TASKLIST_WORK=
# GTD_TASKLIST_WAITING_FOR=
# GTD_TASKLIST_REFERENCE=
